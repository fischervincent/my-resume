---
export interface Props extends astroHTML.JSX.AnchorHTMLAttributes {
  title: string;
  id: string;
}

const {title, id, class: className} = Astro.props;
---
<section class={className} id={id}>
  <div class="my-16 mt-8 sm:mt-16 z-50 relative">
      <h1>{title}</h1>
    <div class="my-2">
      <slot />
    </div>
  </div>
</section>

