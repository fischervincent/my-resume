---
export interface Props {
  title: string;
  id: string;
}

const {title, id} = Astro.props;
---
<section class="my-16" id={id}>
  <h2>{title}</h2>
  <div class="my-2">
    <slot />
  </div>
</section>

